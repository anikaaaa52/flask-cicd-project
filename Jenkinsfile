pipeline {
    agent any
    
    stages {
        stage('Test') {
            steps {
                echo 'Git checkout works!'
                echo 'Pipeline is running successfully!'
                sh 'ls -la'
                sh 'pwd'
            }
        }
    }
}
